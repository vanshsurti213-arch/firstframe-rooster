
  # Creator Profile Board Design

  This is a code bundle for Creator Profile Board Design. The original project is available at https://www.figma.com/design/HqdREndvQ7FBbnsvscRRqu/Creator-Profile-Board-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  